# Changelog

## 0.3.0 — 2026-09-17

- Add persistent pinned tools toolbar (`Quick Tools`).
- Add pin action to search results.
- Reuse original command icons when available, with fallback pin icons.
- Persist pin metadata in SketchUp preferences.
- Improve `run_by_key` by rebuilding the command index when a saved target is not yet available.
- Keep aliases and pinned tool metadata synchronized.
